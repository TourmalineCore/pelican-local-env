# Pelican local env, AGPL-3.0 license

extraConfigMapEnvVars:
  SYNCHRONIZER_SCHEDULE: "{{ .Values.PELICAN_SYNCHRONIZER_SCHEDULE }}"
  SYNCHRONIZER_ENABLED: "{{ .Values.PELICAN_SYNCHRONIZER_ENABLED }}"
  